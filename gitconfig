[user]
	name = pierzakp
    email = piotrek.pierzak@gmail.com
[core]
    excludesfile = /Users/pierzakp/.gitignore
	editor = vim
[alias]
    ci      = commit
    cm      = commit -m
    cmn     = commit -n -m
    cma     = commit --amend
    s       = status -sb
    st      = status
    ls      = log -n 1 --format=%H
    aa      = add --all
    d       = diff
    dnp     = !git --no-pager diff
    ds      = diff --staged
    dsnp    = !git --no-pager diff --staged
    kraken  = !"/Applications/GitKraken.app/Contents/MacOS/GitKraken -p $(pwd)"
[color]
    ui = auto
[hub]
    protocol = https
